# Python 3.9+ required - see runtime.txt

# Masumi payment network integration (requires 0.1.41+ for run() API)
# Option 1: Install from PyPI (if updated version is published)
#masumi==0.1.41

# Option 2: Install from git repository (using updated version from dev branch)
# Using HTTPS format (required for Railway - SSH doesn't work without keys)
# For local development with SSH keys, you can use: git+ssh://git@github.com/masumi-network/pip-masumi.git@dev
git+https://github.com/masumi-network/pip-masumi.git@dev
python-dotenv>=1.0.0
uvicorn>=0.27.0
fastapi>=0.109.0

# Phoenix ML model dependencies (optional - install for recommendations)
# Uncomment below if you want to use ML-based recommendations
# jax==0.8.1
# dm-haiku>=0.0.13
# numpy>=1.26.4

# Additional utilities for production (optional)
# redis  # Uncomment for caching in production
# prometheus-client  # Uncomment for metrics monitoring
